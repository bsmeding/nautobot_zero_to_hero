# Nautobot Zero to Hero - Install instructions
(TLDR version)

## Install task

```bash
    bash install_task.sh
```

## Install all

```bash
    task install:all
```

Details can be obtained using `task --summary install:all`.

## Dry run

Want to see what a task does without executing it?

```bash
    task -n <task_name>
```